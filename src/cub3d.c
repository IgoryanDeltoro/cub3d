#include "../includes/cub3d.h"

int main(int ac, char *av[])
{
    t_game game;

    if (ac != 2)
        return (printf("There are not right args!\n"), 1);
    //check inputs map
    //init game

    return (0);
}